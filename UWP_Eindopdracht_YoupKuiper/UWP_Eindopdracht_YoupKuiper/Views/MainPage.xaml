<Page
    x:Class="UWP_Eindopdracht_YoupKuiper.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_Eindopdracht_YoupKuiper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView x:Name="MyList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <StackPanel>
                            <Rectangle HorizontalAlignment="Left" VerticalAlignment="Stretch" Height="10" Width="35" Fill="CornflowerBlue"></Rectangle>

                            <TextBlock Text="{Binding title}" ></TextBlock>
                            <TextBlock Text="{Binding content}" ></TextBlock>
                        </StackPanel>

                    </Grid>  
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
